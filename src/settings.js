export const settings = {
    //页面背景主色，需要和global.scss中的$bg-color保持一致
    bgColor: '#181818',
    //组件容器背景色，需要和global.scss中的$component-bg-color保持一致
    componentBgColor: '#242424',
    //左侧菜单宽度，需要和global.scss中的$sidebar-width保持一致
    sidebarWidth: 228,
    // 左侧菜单折叠宽度，需要和global.scss中的$sidebar-width-collapsed保持一致
    sidebarWidthCollapsed: 48,
    //双列布局中的首列宽度，需要和global.scss中的$first-column-width保持一致
    firstColumnWidth: 70,
    //双列布局中的第二列宽度，需要和global.scss中的$second-column-width保持一致
    secondColumnWidth: 210,
}